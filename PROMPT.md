# 布布一二风 · 可爱记账 App —— 构建 Prompt（v3 定稿）

> 这份文档本身就是本项目的"施工图"。把整篇丢给一个 AI 编码代理，它能依此产出当前仓库这套代码。
> v1→v3 的演进记录见文末「迭代留痕」，每一版改动都是被真实评测数据逼出来的。

---

## 0. 角色与目标

你是一名全栈工程师兼 UI 设计师。交付一个能直接 `npm run dev` 跑起来的记账 App：

- **移动端优先**，界面是「布布一二」那种极简治愈的可爱风
- 核心亮点功能：**上传消费凭证图片 → AI 自动判断实际花了多少钱 → 生成对应记账条目**
- 所有插画贴图**从互联网搜集**，不自行绘制

## 1. 硬性约束（违反任意一条即为不合格）

1. **不要自己画插图，一个像素都不行。** 所有插画/图标一律从互联网搜集下载、落盘到 `public/bubu/`，离线打包使用。采集要覆盖足够多的生活场景（吃饭、奶茶、通勤、购物、居家、娱乐、医疗、学习、宠物、旅行、人情、发工资、数钱、开心、委屈、打招呼、晚安、庆祝、AA 分账、主插画 ≥ 20 组关键词）。
   - **禁止用 CSS/SVG/Canvas 自绘角色或图标来"兜底"**，也禁止用文字拼一个图形顶上。素材缺失时的正确做法是退回 emoji 占位并提示用户去采集，而不是补一套自制插画。
   - 这一条是硬红线：交付前用 `grep` 扫一遍源码，确认没有任何自绘图形组件残留。
   - 唯一允许的"绘制"是**数据可视化**（环形图、柱状图、进度条）与界面装饰（圆角、阴影、虚线边框）—— 它们表达数据/结构，不是角色插画。
2. **API Key 只允许存在于服务端** `.env.local`。前端 bundle 中不得出现 `sk-` 字符串，交付前用 `grep`/`Select-String` 验证。
3. 视觉识别必须走「**服务端代理 + base64 内联**」。实测把外链 URL 交给上游会被拒（`Failed to download image`），必须读成 base64 dataUrl 再传。
4. **所有金额以「分」为整数存储与运算**，禁止浮点累加、禁止用 `0.1+0.2` 那套算钱。仅在展示层转成元。
5. **识别结果必须可编辑、可撤销，低置信度必须显式提示**，不允许静默入账。
6. 不得声称执行过未执行的验证。每个"完成"都要附上真实命令与真实输出。做不到就明说做不到。

## 2. 技术路径

| 层 | 选型 | 为什么这么选 |
|---|---|---|
| 前端 | Vite + React 18 + TypeScript + Tailwind 3 | 构建快、类型安全；Tailwind 便于把可爱风做成设计令牌而不是散落的魔法数字 |
| 后端 | Node ESM + Express 5，挂在 Vite 的 `/api` 代理后 | 为了托管密钥，前端不能直连模型接口 |
| 存储 | SQLite（better-sqlite3），**初始化失败自动降级 JSON 文件** | 统计要聚合查询；但原生模块可能装不上，接口层必须与驱动无关 |
| 识别 | `deepseek-flash`（OpenAI 兼容 `/chat/completions`） | 支持图片输入，`response_format: json_object` 可强约束输出 |
| 图片处理 | sharp（服务端归一化）+ Canvas（浏览器侧预压缩） | 控制上行体积，统一转 JPEG，避免 HEIC/WebP/动图踩坑 |
| 图表 | 纯 CSS/SVG 绘制**数据可视化**（conic-gradient 环形图 + flex 柱状图） | 这是表达数据的图表，不属于角色插画；不引 ECharts，产物体积小且更容易做圆角可爱风 |

**路由**：`#/home`、`#/scan`、`#/stats`、`#/settings`，加 `?add=1` / `?edit=<id>` 深链打开记账弹层。

## 3. 视觉规范：布布一二风

**设计令牌（写进 Tailwind theme，不要在组件里硬编码色值）**

- 主色：奶油白 `#FFFDFA` / `#FFF9F2` / `#FFF3E6` / `#FFEBD8`
- 点缀：蜜桃粉 `#FFE3DD` / `#FFCDBF` / `#FFB3A7` / `#FF9A87` / `#FF8E7A` / `#F5725C`
- 辅助：浅杏 `#FFE0B2`、薄荷 `#BFE3D0`、雾蓝 `#C9DDF0`、丁香 `#DED3F2`
- 文字：暖褐 `#5B4A42`，次级 `#8C776C` / `#B7A69C` / `#D9CCC4`。**不用纯黑 `#000`。**
- 圆角：卡片 28px、按钮全圆（999px）、输入框 20px
- 阴影：`0 6px 0 rgba(91,74,66,.08)` —— 用零模糊的"贴纸厚度"而不是高斯阴影
- 描边：2px 暖褐半透明；重点卡片用 3px 白边 + 2px 粉描边模拟贴纸白边
- 字体：`LXGW WenKai`（霞鹜文楷）优先，回退系统圆体

**必须实现的可爱风组件**

- `.card` 便利贴卡片 / `.sticker-card` 贴纸描边卡片 / `.ticket` 票根撕裂边（左右半圆缺口用 `::before/::after` 画）
- 手绘感圆角：`border-radius: 30px 26px 32px 24px / 26px 32px 24px 30px`（四角不等，模拟手绘）
- 动效：按钮按下 `scale(0.94)`；弹层 `slideUp`；卡片进场 `popIn` 带轻微回弹；角色贴图 `floaty` 呼吸浮动；记账成功从点击坐标掉落爱心/星星粒子（Web Animations API，无依赖）
- 贴图统一 `object-contain` + `drop-shadow`，找不到图时退化渲染 emoji，**UI 永远不能出现破图**

## 4. 页面清单

| 页面 | 关键内容 |
|---|---|
| 首页 `#/home` | 主插画 + 本月结余大字 + 预算进度条 + 今日/本月收支三宫格 + 4 个分类快捷记账 + 按日期分组的流水（点击展开「改一改／删掉」） |
| 记一笔（弹层） | 收支切换、超大金额输入、分类九宫格（贴图）、心情表情、商户/备注/时间；编辑模式复用同一弹层 |
| 拍照记账 `#/scan` | 拖拽 / 点击 / **Ctrl+V 粘贴** 三种上传方式 → 缩略图队列 → 识别进度条 → 结果确认卡片 |
| 统计 `#/stats` | 环形图 + 分类占比条 + 每日柱状图 + 分类排行榜 + 收支对比；可切月份 |
| 设置 `#/settings` | 预算修改、AI 服务状态（密钥/模型/prompt 版本/存储驱动）、导出 JSON、清空数据、版权说明 |

## 5. 视觉识别模块（核心）

### 接口

```
POST /api/vision/recognize
body: { images: string[], hint?: string, promptVersion?: string, useCache?: boolean }
```

- `images`：dataUrl 或纯 base64，**最多 4 张**；多张视为"同一笔单据的补充图"一起送入
- 服务端流程：体积/格式校验 → sharp 归一化（长边 ≤1536、转 JPEG q=82、自动旋转）→ 内容哈希查缓存
  → 调 `deepseek-flash` → 剥离 markdown 围栏后解析 JSON → **金额对账** → 置信度压制 → 返回

### 强制输出结构

```json
{
  "is_bill": true,
  "doc_type": "paper_receipt|payment_screenshot|order_page|menu|price_tag|other",
  "merchant": "",
  "total": 0,
  "currency": "CNY",
  "date": "YYYY-MM-DD",
  "time": "HH:mm",
  "items": [{ "name": "", "price": 0, "qty": 1 }],
  "suggested_category": "food|drink|transport|shopping|home|fun|health|study|pet|trip|gift|salary|bonus|refund|other",
  "confidence": 0.0,
  "warnings": []
}
```

### Prompt 里必须写死的规则

1. **判断顺序**：先判断"这是不是能看出金额的消费凭证"，不是就直接 `is_bill=false`、`total=null`、`confidence≤0.3`，不许硬凑。
2. **金额优先级**：实付 > 合计/总计 > 应收 > 单价×数量。
3. **折扣**：有满减/优惠券时 `total` 取折后实付值，既不取原价也不取优惠额；多金额易混时在 `warnings` 说明选了哪个、放弃了哪个。
4. **零编造**：数字遮挡/反光/模糊到无法确认 → `total=null`、`confidence≤0.35`、`warnings` 写明。禁止编造图中不存在的商户名、商品名、日期。
5. **日期**：只能用图上真实出现的时间，没有就 `null`，**禁止用今天日期顶替**。
6. **金额对账**：`items` 求和不等于 `total` 时以 `total` 为准，并写入 warning。
7. 必须给 few-shot 示例（支付截图 / 带折扣小票 / 非账单插画各一个），`temperature=0`。

### 服务端后处理（比 prompt 更靠得住的一层保险）

- `total` 为 null 但 items 有值时，按明细求和并在 warning 说明
- `total` 为 null 时**强制** `confidence = min(confidence, 0.2)`
- 分类不在枚举内 → 归入 `other` 并记 warning
- `needs_review = !is_bill || confidence < 0.6 || warnings.length > 0`
- 单张超时 60s、指数退避重试 2 次；4xx（除 429）不重试；最终失败返回结构化错误而不是抛栈
- 相同图片内容 + 相同 prompt 版本命中内存缓存（LRU 60 条）

### 前端交互

- 识别中：进度条 + 贴图浮动动画 + 缩略图
- 结果页：置信度分级文案（「看得很清楚，应该没跑～」/「我不敢打包票，帮我核对一下金额哦」/「这张图我没看出消费金额」）
- 金额输入框低置信度时高亮描边；旁边给一个「AI 读到的 ¥X（点一下还原）」按钮
- 显示 `warnings` 列表、识别方式、把握度、耗时、prompt 版本
- 原图缩略图始终可见，便于对照
- **点「确认记账」才落库**

## 6. 贴图采集流水线

`scripts/fetch-assets.mjs` + `scripts/review-assets.mjs`：

1. 读 `scripts/asset-plan.json` 的「关键词 → 用途」映射
2. 请求 `https://cn.bing.com/images/async?q=<kw>&first=1&count=35&mmasync=1`，正则提取 `murl&quot;:&quot;(.*?)&quot;`
3. 逐条尝试下载（带 UA/Referer，超时 20s），校验魔数（FFD8FF / 89504E47 / WEBP / ftyp）且 > 20KB
4. 内容 SHA-256 去重；同一图床最多取 4 张，避免风格单一（实测素材高度集中在 `duitang`/`dtstatic`）
5. 清洗：sharp 读真实像素，**同一颜色占比 > 92% 判为 logo/占位图剔除**，长边 < 160 或长宽比 > 3.2 剔除，四角同色则 trim 掉多余留白，合成到奶油白底，导出 512px WebP
6. 视觉筛选（并发 4）：逐张问模型"你实际看到什么"+"有没有文字水印/多图拼接/是不是截图/像不像卡通/质量分"，`is_cartoon && !水印 && !拼图 && quality≥5` 才算可用
7. 产出 `manifest.json`（含每张图的判定依据）与 `_contact-sheet.html` 预览墙，人工一眼验收

**关键工程细节**：原图放 `assets-src/raw/`，**不要放 `public/`** —— Vite 会把 `public/` 原样拷进 `dist/`，否则 30MB 原始素材全进产物。

## 7. 迭代协议（必须执行并留痕）

1. 每轮改动写 `logs/iter-<n>.md`：本轮目标、改了什么、命令、真实输出、发现的问题、下一轮动作。
2. 用固定夹具 `fixtures/bills/` 逐张跑 `npm run eval:vision`，输出：金额完全正确率、分类正确率、非账单误判率、需人工确认占比、平均/最慢耗时、失败数。
3. **夹具的标准答案必须能自证**：先用文生图生成凭证图，再让视觉模型读出金额，只有读出的金额与预设一致才收为夹具（最多重试 3 次）。否则夹具本身是错的，评测没有意义。
4. 评测必须走本地 API 真实链路（不是直连上游），且 `useCache=false`，否则吃缓存等于没测。
5. 任一项指标退化必须回滚并在日志写明。
6. 至少三轮：v1 裸 prompt → v2 强 schema → v3 few-shot + 对账 + 拒答兜底。

## 8. 验收标准

- `npm install --ignore-scripts` 与 `npm run build` 均 exit 0
- 首页、记一笔、拍照记账、统计、设置五处均可交互且无 console error
- 端到端：上传一张支付截图 → 出结果 → 金额与截图一致 → 确认后列表出现该笔 → 可编辑可删除
- `dist/` 中无任何 `sk-` 字符串
- 贴图预览墙 ≥ 16 张可用素材，`manifest.json` 完整
- README 写清启动方式、Key 配置、素材来源与版权说明（**仅供个人学习使用，不商用、不再分发**）

## 9. 已知环境坑（Windows + 受限沙箱，照抄即可少走弯路）

1. `npm install` 默认缓存目录在工作区外会被沙箱拒绝 → 加 `--cache .npm-cache`，并加 `--ignore-scripts` 绕开生命周期脚本的 `spawn EPERM`。
2. PowerShell 的 `Invoke-WebRequest` / `curl.exe` 在本机无法联网，**只有 Node 的 `fetch` 能出网**。所有 HTTP 请求都写成 `.mjs` 用 node 跑。
3. Vite 在 Windows 上会 `spawn` 子进程做真实路径检查，esbuild 也需要管道启动 service，受限沙箱下会 `spawn EPERM` → 需要在放宽权限的环境构建/起服务。
4. Express 5 的同步抛错会被自动捕获转交错误中间件（无需 `try/catch` 包每一层）。
5. 服务端脚本用 `.mjs`，**不能带 TypeScript 类型注解**（`export type` 会直接语法报错），类型信息写 JSDoc 注释。

## 10. 两个必须记住的踩坑（都是实测撞出来的）

### 10.1 用 `response_format: json_object` 时，prompt 里必须出现 "json" 字样

上游有一条硬校验：prompt 文本里不含 `json`（不区分大小写）就直接 400：
`Prompt must contain the word 'json' in some form to use 'response_format' of type 'json_object'`。

中文里写"只输出 JSON 结构"一旦被本地化改写掉就会全线 400。工程上要加两道保险：

- prompt 里显式写小写 `json`
- 服务端发请求前自检，命中就抛结构化错误（`PROMPT_MISSING_JSON_KEYWORD`），而不是等上游 400
- 4xx 不要笼统包成"上游错误"，要把上游 message 原样透传，否则排查全靠猜

### 10.2 `public/` 下的 `.html` 会被 Vite 当成构建入口

把人工验收用的 `_contact-sheet.html` 放在 `public/bubu/` 会导致：Vite 依赖扫描把它当入口处理，
构建时又被原样拷进 `dist/`（不该发布的东西进了产物）。正确做法是放到 `public/` 之外，用 `file://` 直接打开。
同理，**贴图原图绝对不要放 `public/`**，否则几十 MB 原始素材会全进产物。

## 11. 验证方法学（这部分比代码更容易翻车）

1. **截图验证必须每页独立 context**。hash 路由跳转不会重新加载页面，React 状态会保留：
   用同一个 page 连拍时，`#/home?add=1` 打开的弹层会一直盖住后面的统计页/设置页截图，
   于是"统计页评审"其实评的是弹层 —— 会得出完全错误的结论。
2. **"保存成功"不等于"用户看到保存成功"**。断言分两层：先查 API 里记录是否真落库，
   再查页面是否渲染出来。端到端测试就靠这个抓到一个真 bug：拍照记账保存后没跳回首页，用户会以为没记上。
3. **有争议的布局问题用几何量测，不靠看图猜**。固定底部导航天然会盖住视口下半部分内容，这属正常表现；
   真正要回答的是"滚到最底部之后还有没有内容被压住"。写脚本滚到底再量
   `最后一个内容块.bottom` 与 `导航栏.top` 的差值，才有确定答案。
4. **贴图上线前要抠底**。简单按亮度做 alpha 会漏掉大量浅色底（半透明白在浅色卡片上仍显示为方块）。
   正确做法是**从四条边做泛洪填充**，只清除与边缘连通的浅色区域，角色内部的白色才不会被误伤。
5. **评测夹具用本地渲染，别用文生图**。文生图金额不可控（还要反向自证），且外部服务会限流（实测 429）。
   用 sharp 直接画小票/支付页，金额精确、可复现、离线可跑，还能精确构造"模糊/遮挡/非账单"等挑战样本。
6. **验证脚本本身也要当代码审**。本轮验证脚本先后出过三个 bug：截图共用 page、断言文案写错、
   请求体里把 `role` 写成 `type` —— 每一个都导致过错误结论。

---

## 迭代留痕

| 版本 | 改动 | 触发原因 |
|---|---|---|
| **v1** | 裸 prompt：「看看这张图，告诉我花了多少钱，用 JSON 回答」 | 起点，用来量化"不约束的模型有多离谱" |
| **v2** | 补上完整字段定义、金额口径（只取实付/合计）、分类枚举、doc_type 枚举、不确定就写 warnings | v1 的字段名和口径飘忽，下游没法解析 |
| **v3** | 加判断顺序（先判是不是账单）、折扣取值规则、多金额取舍、零编造红线、日期禁顶替、金额对账、三个 few-shot 示例、`temperature=0`；服务端叠加置信度压制与 needs_review | v2 仍会把菜单、风景照当账单，且金额对不上明细时沉默 |
| **v3.1** | 修复 prompt 缺 `json` 字面量导致上游 400；加服务端自检；错误信息透传上游 message | 实测 v2/v3 全线 400，根因是上游的 json 关键字校验 |

### UI / 素材迭代（由截图视觉评审驱动）

| 轮次 | 发现问题 | 处置 |
|---|---|---|
| 1 | 分类图标带白色方块、像实拍截图 | 筛选门槛 `quality>=5` → `>=6` 且要求 `bg_clean`；组件按质量分降序取图 |
| 2 | 白色方块仍在 | 改为四边泛洪抠底，平均 27.7% 面积转透明 |
| 3 | 「每天花了多少」柱状图整张空掉（只剩一根柱） | 补齐整月每一天，零消费的天画成矮条 |
| 4 | 图例只列前 5 类，剩余占比无说明 | 改为前 4 类 + 「其他」聚合，占比合计 100% |
| 5 | 疑似底部导航遮挡最后一条 | 几何量测确认滚到底后有 91px 净余量、零遮挡（视觉评审在视口截图上的误判）；仍把 `pb` 定为 176px |
| 6 | 拍照页下半页大片空白 | 补「三步就记好一笔」引导卡；原图缩略图放大并可点开看大图 |

> 真实评测数据见 `logs/iter-*.md`（每轮含逐张明细与指标对比表）。
> 界面验证见 `logs/ui-verify.md`、`logs/ui-visual-review.md`、`logs/layout-metrics.md`。
